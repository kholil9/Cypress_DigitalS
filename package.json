{
  "name": "cypress_frame",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "e2e:chrome": "cypress run --browser chrome",
    "report:merge": "mochawesome-merge cypress/results/*.json -o cypress/report.json",
    "report:generate": "marge cypress/report.json --charts true",
    "mochawesome:generate": "npx marge cypress/results/mochawesome.json -o cypress/reports -f final-report --cdn true",
    "mochawesome:merge": "npx mochawesome-merge cypress/results/*.json > cypress/results/mochawesome.json",
    "cy:run": "cypress run",
    "cy:verify": "cypress verify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^2.3.0",
    "cypress": "^14.0.3",
    "cypress-file-upload": "^5.0.8",
    "cypress-mochawesome-reporter": "^4.0.0",
    "cypress-real-events": "^1.14.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}